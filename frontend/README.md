# Frontend

React + TypeScript + Vite frontend.

## Run local
1. Install dependencies:
   `npm install`
2. Start dev server:
   `npm run dev`

## Purpose in Phase 0
- Prompt-to-workflow generation UX.
- JSON workflow review and versioning.
- Manual trigger execution and run-step timeline.
- Connector account management.
